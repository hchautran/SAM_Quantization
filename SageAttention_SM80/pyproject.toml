[build-system]
requires = ["setuptools", "wheel", "torch", "packaging"]
build-backend = "setuptools.build_meta"

[project]
name = "sageattention-sm80"
version = "1.0.0"
description = "Minimal SM80-only version of SageAttention"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "Apache-2.0"}
